package CPAN::Testers::Schema::LangReleases;
use Mojo::Base 'Yancy::Model::Schema', -signatures;

# XXX: Yancy command to generate model classes

sub get_or_create( $self, $row ) {
    # XXX: Create Lang if needed
}

1;
